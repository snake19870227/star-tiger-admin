input {
  tcp {
    port => 5044
    codec => json_lines
  }
}

output {
  elasticsearch {
    hosts => ["http://adminElasticSearch19200:9200"]
    index => "admin-web-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}

